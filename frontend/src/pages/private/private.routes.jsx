// Router
import {
  BrowserRouter,
  Routes,
  Route,
  RouterProvider,
  Navigate,
} from "react-router-dom";

const PrivateRoutes = () => {
  <BrowserRouter>
    <Routes></Routes>
  </BrowserRouter>;
};

export default PrivateRoutes;
